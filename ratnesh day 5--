#include<stdio.h>
int main()
{
	int n,d,s=0,p=1;
	printf("Enter any no ");
	scanf("%d",&n);
	while(n>0)
	{
		d=n%10;
		s=s+d;
		p=p*d;
		n=n/10;
	}
	printf("Sum = %d  Product = %d\n",s,p);
	if(s==p)
	printf("Number is a SPY number");
	else
	printf("Number is not a SPY number");
	
	
	
	return(0);
}
